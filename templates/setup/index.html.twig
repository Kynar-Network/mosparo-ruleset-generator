{% extends 'base.html.twig' %}

{% block title %}Setup - RuleSet Generator for mosparo{% endblock %}

{% block body %}
<div class="container py-4">
    <header class="mb-5 text-center">
        <h1>Setup</h1>
        <p class="text-muted">Before you can use the RuleSet Generator, you need to set up your environment.</p>
    </header>

    <div class="card mb-4 rounded shadow-sm">
        <div class="card-header bg-dark text-white d-flex justify-content-center">
            <h5>Generate Required Information</h5>
        </div>
        <div class="card-body">
            <div class="d-flex justify-content-center"><p>To generate required information, click the button below. This will create secure random secret keys and save it to your .env file.</p></div>
            <form id="setupForm" method="POST" action="{{ path('app_setup_generate') }}">
                <div class="d-flex justify-content-center"><button type="submit" class="btn btn-primary">Generate Now</button></div>
            </form>
        </div>
    </div>
</div>
{% endblock %}
